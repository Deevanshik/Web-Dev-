const posts = [
  { id: 1, title: "One" },
  { id: 2, title: "Two" },
  { id: 3, title: "Three" },
];

const getPosts = () => posts;

const getPostLength = () => posts.length;

export { getPostLength };
export default getPosts;
